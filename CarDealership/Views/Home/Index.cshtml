@{
    ViewData["Title"] = "Acasă";
}

<div class="text-center mt-5">
    <h1 class="display-4">Bun venit la CarDealership!</h1>
    <p>Sistemul de gestiune auto pentru agenții de vânzări.</p>

    @if (User.Identity?.IsAuthenticated != true)
    {
    <a class="btn btn-primary mt-3" asp-controller="Auth" asp-action="Login">Autentificare Agent</a>
    }
</div>